---
# ============ 必填 ============
title: '在这里写标题'
date: '2026-09-24'          # 决定排序和首页的年份分组；引号别去掉
# ============ 建议填 ============
summary: '一句话说清这篇讲什么。会出现在首页卡片、搜索引擎描述和 RSS 摘要里。'
# ============ 可选 ============
tags: ['操作系统', '学习笔记']    # 用新标签会自动生成对应的标签页
cover: '/og-cover.svg'          # 分享卡片配图；不需要就整行删掉
draft: true                     # ★★ 写完改成 false 才会发布到线上
---

<Callout type="warn" title="使用说明（写完后把这一段删掉）">
  1. **改文件名**：英文小写 + 连字符，例如 `operating-system-notes.mdx`——**文件名就是网址**（`/blog/operating-system-notes`）。
  2. **改 frontmatter**：上面的 title / date / summary / tags。
  3. **写正文**：从 `##` 开始，不要用 `#`，页面标题已经由 title 提供了。
  4. **发布**：把 `draft` 改成 `false`，然后 `git push`。
</Callout>

## 这篇要解决什么问题

先用两三句话交代背景：为什么会遇到这个问题、最后的结论是什么。

<Callout type="tip" title="结论写在前面">
只想知道答案的读者，看完这一段就可以走了；后面的内容是推导过程和细节。
</Callout>

## 正文第一节

正文写在这里。能用 **加粗**、*斜体*、`行内代码`、~~删除线~~，以及 [站内链接](/tags) 和 [外部链接](https://nextjs.org)。

### 需要时可以拆更细的小节

- 无序列表项
- 需要强调时用 **加粗**，别整段加粗

1. 有序步骤一
2. 有序步骤二
3. 有序步骤三

## 代码示例

```ts
// 代码块标注语言就会自动高亮
export function greet(name: string) {
  return `你好，${name}`;
}
```

```bash
# 命令行示例
npm run dev
```

## 对比表格（不需要就整段删掉）

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| 方案 A |  |  |
| 方案 B |  |  |

<Callout type="info">
三种提示框都可以用：把 type 换成 tip / warn / info，不写 title 则用默认标题。
</Callout>

## 踩坑记录

- [x] 已经踩过并解决的
- [ ] 还没验证的
- [ ] 待补充

> 收尾可以放一句引用；不需要就删掉。

## 图片（可选）

![图片说明](/og-cover.svg)

## 小结

三行以内，把这篇的要点再收一遍。写完之后别忘了两件事：**删掉顶部的使用说明**、**把 draft 改成 false**。
